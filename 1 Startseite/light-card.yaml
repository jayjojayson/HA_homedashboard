type: custom:mushroom-template-card
primary: PC-Led
entity: switch.pc_led_pv_led
icon: mdi:led-strip
name: PC-Led
icon_color: |-
  {% if is_state('switch.pc_led_pv_led', 'on') %}
    #ffc107
  {% else %}
    grey
  {% endif %}
visibility:
  - condition: state
    entity: switch.pc_led_pv_led
    state: "on"
grid_options:
  columns: 4
  rows: 1
card_mod:
  style: |
    mushroom-shape-icon {
      --icon-size: 45px !important;
      background: none !important;
      box-shadow: none !important;
    }
